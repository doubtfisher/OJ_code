题目：
请编写一个函数，将两个数字相加。不得使用+或其他算数运算符。给定两个int A和B。请返回A＋B的值。


分析：
1.二进制位异或运算相当于对应位相加，不考虑进位
2. 二进制位与运算相当于对应位相加之后的进位
3. 两个数相加：对应二进制位相加的结果 + 进位的结果，循环判断，当进位之后的结果为0时，相加结束。



class UnusualAdd {
public:
    int addAB(int A, int B) {
        int m=A^B;//两个数相加，不考虑进位的结果
        int n=(A&B)<<1;//两个数相加的进位
        while(n!=0)//当进位为0的时候，相加结束
        {
            A=m;
            B=n;
            m=A^B;
            n=(A&B)<<1;
        }
        return m;
    }
};